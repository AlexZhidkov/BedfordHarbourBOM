<core:UserControlViewBase x:Class="Bom.Desktop.Views.StockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:core="clr-namespace:Core.Common.UI.Core;assembly=Core.Common.UI"
             xmlns:conv="clr-namespace:Bom.Desktop.Support"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <Button Content="Add New" Command="{Binding AddStockCommand}" />
            </StackPanel>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80" SharedSizeGroup="links" />
                    <ColumnDefinition Width="150" SharedSizeGroup="cost" />
                    <ColumnDefinition Width="80" SharedSizeGroup="count" />
                    <ColumnDefinition Width="60" SharedSizeGroup="countDate" />
                    <ColumnDefinition Width="130" SharedSizeGroup="notes" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="1" Text="Cost" />
                <TextBlock Grid.Column="2" Text="Count" />
                <TextBlock Grid.Column="3" Text="Count Date" />
                <TextBlock Grid.Column="4" Text="Notes" />
            </Grid>
            <ItemsControl Grid.Row="2" ItemsSource="{Binding Stocks}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80" SharedSizeGroup="links" />
                                <ColumnDefinition Width="150" SharedSizeGroup="cost" />
                                <ColumnDefinition Width="80" SharedSizeGroup="count" />
                                <ColumnDefinition Width="60" SharedSizeGroup="countDate" />
                                <ColumnDefinition Width="130" SharedSizeGroup="notes" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <TextBlock>
                                    <Hyperlink Command="{Binding DataContext.EditStockCommand, RelativeSource={RelativeSource AncestorType=core:UserControlViewBase}}" CommandParameter="{Binding}">
                                        <TextBlock Text="edit" />
                                    </Hyperlink>
                                </TextBlock>
                                <TextBlock Text=" " />
                                <TextBlock>
                                    <Hyperlink Command="{Binding DataContext.DeleteStockCommand, RelativeSource={RelativeSource AncestorType=core:UserControlViewBase}}" CommandParameter="{Binding}">
                                        <TextBlock Text="delete"/>
                                    </Hyperlink>
                                </TextBlock>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding Part.Description}" />
                            <TextBlock Grid.Column="2" >
                                <Hyperlink Click="UpdateStock_Click" Tag="{Binding}">
                                    <TextBlock Text="{Binding Count}"/>
                                </Hyperlink>
                            </TextBlock>
                            <TextBlock Grid.Column="3" Text="{Binding CountDate, StringFormat=dd-MM-yyyy}" />
                            <TextBlock Grid.Column="4" Text="{Binding Notes}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</core:UserControlViewBase>
